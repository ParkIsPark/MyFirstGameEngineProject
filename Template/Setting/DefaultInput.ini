# DefaultInput.ini — input bindings (UPlayerController::SetupDefaultBindings).
[ActionMappings]
Jump   = Space
Sprint = LeftShift

[AxisMappings]
MoveForward = W:+1.0, S:-1.0
MoveRight   = D:+1.0, A:-1.0

[MouseLook]
Sensitivity = 0.15
